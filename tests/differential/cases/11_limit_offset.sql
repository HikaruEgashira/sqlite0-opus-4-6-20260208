CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT, price INTEGER);
INSERT INTO items VALUES (1, 'alpha', 100);
INSERT INTO items VALUES (2, 'bravo', 200);
INSERT INTO items VALUES (3, 'charlie', 300);
INSERT INTO items VALUES (4, 'delta', 400);
INSERT INTO items VALUES (5, 'echo', 500);
SELECT * FROM items LIMIT 3;
SELECT * FROM items LIMIT 2 OFFSET 2;
SELECT * FROM items ORDER BY price DESC LIMIT 3;
SELECT * FROM items ORDER BY price DESC LIMIT 2 OFFSET 1;
SELECT name FROM items ORDER BY name ASC LIMIT 3;
